# Backend service dependencies (subset of the repo-root requirements.txt).
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic-settings>=2.2
python-multipart>=0.0.9
httpx>=0.27
alembic>=1.13

# RAG pipeline (imported by backend.services.pipeline)
faiss-cpu>=1.8
numpy>=1.26
sentence-transformers>=2.7   # optional at runtime; hash fallback if absent
